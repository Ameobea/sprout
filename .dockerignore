# Allowlist: the image only needs the server output (built outside docker), deps manifests, and data files
*
!package.json
!yarn.lock
!build
!data
data/*
!data/processed-metadata.csv
!data/projected_model_embedding.json
